'use strict';

let header = document.querySelector('.page-header');
let headerControls = header.querySelector('.trip-main');
let tripViewTitle = headerControls.querySelector('.trip-view');

let menu = () => {
  return (``);
};
